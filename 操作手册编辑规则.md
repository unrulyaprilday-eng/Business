# B端后台操作手册编辑规则

本规则用于维护 `B端后台操作手册.docx`。后续编辑手册时先读本文件，按既定目录、格式和脚本流程处理，避免重复探索 Word 结构、截图处理方式和样式规则。

本文件只记录通用规则、目录骨架、格式要求和执行流程；不要写入具体模块正文、字段说明、业务描述、FAQ 答案或某次生成出来的详细内容。

## 手册请求快速规则

处理任何“操作手册 ...”请求时，默认直接套用下面这组固定规则：

- 只改手册：除非用户明确要求同步原型，否则只修改 `B端后台操作手册.docx`、手册脚本和手册图片资产。
- 先图后文：用户聊天里一旦给了截图，先提取原图，再写正文；不要先生成示意图、不要先截 HTML、不要先讨论替代图源。
- 固定图源优先级：当前会话原图 > 当前线程历史原图 > 本机 Codex 日志中的原图 > 用户重新上传或提供路径。
- 固定提图动作：定位带图 `userMessage` -> 提取 `data:image` -> 落盘到 `custom/assets/manual-模块名/` 或 `custom/assets/manual-模块名-original/` -> 核图 -> 让更新脚本引用该原图。
- 固定脚本策略：一旦确认聊天原图可用，手册更新脚本只引用原图文件，不再保留自动生成示意图、自动截 HTML 或其他 fallback 图源分支。
- 固定省 token 原则：优先复用 `scripts/extract_chat_images_from_codex_log.py`，优先读取线程和日志，不扫缓存，不反复打印大段日志，不在对话里输出 base64。

只有在“线程历史 + 本机日志”两条路径都拿不到完整原图时，才允许进入最后兜底：优先在文档放图位置保留占位和图标标注；只有用户明确要求必须补齐原图时，才再向用户要本地图片路径或请用户重新发图。

## 手册目录骨架

当前操作手册默认按商户后台真实菜单结构组织，一级目录使用：

```text
一、手册说明
二、登录与基础导航
三、首页
四、站点配置
五、运营中心
六、优惠活动
七、财务中心
八、数据报表
九、游戏中心
十、用户管理
十一、代理中心
十二、风险中心
十三、商户中心
十四、埋点配置
十五、人事中心
十六、典型业务流程
十七、常见问题与排查
```

目录归属规则：

- 手册主体按后台左侧菜单的真实模块和页面顺序执行，不按抽象业务场景重新归类。
- 页面写到其真实所在菜单模块下。比如 `模板管理`、`站点应用配置/站点设置` 如果实际在 `站点配置` 下，就写在 `站点配置` 章节内。
- 跨模块业务场景不要挪动主体页面归属，统一放到 `典型业务流程` 章节串联说明。
- 如果后台菜单名称与业务名称不同，章节标题优先使用后台菜单名称；业务名称可在正文中说明。
- `风险中心` 只写玩家侧风险和风控处理，不写后台账号安全。
- `人事中心` 单独成章，用于后台账号、角色选择、登录日志、操作日志、异常日志等。
- 商户侧只选择系统预设角色，不写成商户可自行配置角色权限。
- `模板管理` 和 `站点设置` 都按正式页面说明处理，不命名为“补充说明”。
- 登录与基础导航中，图2是“顶部导航栏右侧区域”，不是“后台通用工具栏”。
- 图2和右侧导航说明放在 `菜单导航` 下方，紧跟左侧目录/顶部基础入口说明；筛选、表格、分页、弹窗、状态开关是后续独立通用操作，不与图2混在一起。

## 统一内容模板

核心模块统一使用以下结构：

```text
模块定位
页面说明
字段说明
操作流程
注意事项
常见问题
```

小页面或配置卡片可使用简化结构：

```text
页面/卡片标题

[图片]
图N：图片说明。

字段名：用途、填写规则、影响范围和检查要点。
操作方式：新增、编辑、删除、启用、停用、保存、预览等路径。
使用要点：上线前复核内容、风险点和联动模块。
```

后续默认页说明模板：

```text
页面/卡片标题

一句话说明页面用途。

[图片]
图：页面或区域说明。

功能点A：直接说明筛选、列表、按钮、弹窗、状态或处理动作。
功能点B：把关键操作流程揉进功能点说明里，不单独拆成长流程。
功能点C：只有报表、统计、明细类页面，才补充必要的核心字段说明。
功能点D：无图时也按“用途 + 功能点列表”成文，不退回“页面功能/主要操作/使用说明”三段式。
```

写作要求：

- 写真实可执行内容，不写空泛介绍。
- 页面说明按真实页面顺序直接展开每个页面的介绍、常用操作和后续补充点；不要先列一张页面清单，再在下面重复按截图介绍。
- 字段说明覆盖实际会看到的关键字段类型。
- 操作流程写成步骤。
- 注意事项写影响范围、复核点、风险点和数据口径。
- 常见问题写排查路径，不只写结论。
- 多卡片、多 Tab、多配置区页面按截图顺序或页面从上到下顺序编写。
- 页面必须写入 `B端后台操作手册.docx` 正文，不能只在脚本、截图资产或规则文档中记录。
- 新增或补充页面时，先根据原型页面 title、截图中的选中态、页签高亮、面包屑和页面标题识别真实页面及当前区域；这些识别依据只用于判断页面归属和截图顺序，不要原样写成正文。
- 识别截图归属时，固定优先看页面 title 和当前 Tab / 页签选中状态：如果同一页面存在多个 Tab、卡片页签或按钮弹窗，先根据 title 判断所属页面，再根据截图里高亮、下划线、选中背景、激活按钮或当前展开区域判断该图对应哪个子模块、哪个 Tab、哪个弹窗。
- 如果同一组截图包含主表、Tab 切换页和按钮弹窗，默认按“页面 title -> 当前选中 Tab -> 当前弹窗标题”三步确认图片归属，再决定文档里的标题、配图顺序和说明内容。
- 只有在 title 和选中态都无法唯一判断时，才再结合面包屑、按钮文案、列表字段和图注上下文辅助识别；不要在没有核对 title 和选中态的前提下直接凭图片顺序下结论。
- 正文面向商户后台使用人员，直接说明页面用途、字段含义、操作步骤、保存结果和注意事项；不要写“根据原型/截图”“页面 title 为”“蓝色下划线表示选中态”“应按当前选中状态识别页面”这类制作过程描述。
- 根据原型和用户截图补充说明文字时，优先描述截图中可执行的业务动作，例如新增、编辑、删除、启用、停用、上传、保存、取消、弹窗字段和列表字段；文案要像商户操作手册，而不是原型验收说明。
- 后续默认优先使用“1 句用途 + 功能点列表”的简洁模板，风格参考 `平台公告`、`客服中心`；即使暂时没有配图，也要写成自然的页面说明，不使用“页面功能 / 主要操作 / 使用说明”这类占位式小标题。

## 编辑路线

### 全量重整或调整大目录

适用场景：

- 用户要求重排整本手册目录。
- 用户要求统一所有核心模块为固定模板。
- 用户要求保留原截图并重新生成清晰结构。
- 需要重新抽取并嵌回已有图片。

优先复用全量脚本：

```powershell
python scripts\reorganize_manual_by_menu_docx.py
```

执行前确认：

- 是否仍使用当前按后台真实菜单组织的目录骨架。
- 是否需要先读取 `data/document.js` 或当前 Word 标题确认真实菜单/页面顺序。
- 是否保留已有截图。
- 是否需要调整脚本中的模块列表、业务流程或 FAQ 骨架。

脚本要求：

- 写入 Word 前先备份。
- 保留并重新嵌入已有截图。
- 使用 `python-docx` 生成或修改 Word。
- 不把具体正文内容写进本 MD；具体内容写在脚本或 Word 中。

### 局部补充页面或模块

适用场景：

- 只补一个页面。
- 只调整一个模块。
- 只插入或替换少量截图。
- 只统一局部样式。

推荐流程：

1. 读取当前章节标题，定位目标章。
2. 只读取目标章附近段落，不打印整本 Word。
3. 参考同章或相邻模块的写法补充内容。
4. 判断是否需要配图：已有截图能复用时优先复用；没有截图且用户只要求补文字时不强行造图；需要补图时按“图片处理规范”生成或嵌入。
5. 插入图片时放到对应页面标题、页面介绍或配置区域说明下方，不把图片集中堆到章节末尾。
6. 保存前备份 Word。
7. 做最小校验。

局部编辑脚本默认优先使用通用脚本：

```text
scripts/update_manual_sections.py
```

推荐方式是“一个通用脚本 + 一个很小的 JSON 配置文件”，而不是每次都新建一次性的页面脚本。只有在以下场景才单独新增 `scripts/update_manual_页面或模块名.py`：

- 需要生成复杂示意图或重绘图片。
- 需要处理非常特殊的表格、页签、分页或批量格式修复。
- 需要做整本手册重排，已经超出局部章节替换范围。

通用脚本配置建议：

```text
scripts/manual-*.json
```

配置文件只放本次要更新的章节标题、正文 blocks、是否保留文档内已有图片、以及可选的聊天原图提取参数。

`keep_existing_media` 使用边界固定如下：

- 只有当目标章节里的图片本来就已经放在正确段落位置，且本次只是小幅补字、微调表述、不改图文顺序时，才允许使用 `keep_existing_media: true`。
- 如果目标章节存在“图片集中堆在节末”“需要按多张图拆成多个子模块”“需要补图注”“需要把说明插到每张图前后”这几类情况，禁止使用 `keep_existing_media: true`。
- 遇到上述情况时，固定做法是先把原图提取或落盘到 `custom/assets/manual-页面名/`，再在 JSON 里显式写 `image` block，并把 `keep_existing_media` 设为 `false`，按线性顺序重建整节。
- 不要因为 Word 里“已经有图”就默认走保留媒体模式；先判断图片位置是否已经满足最终版式要求。

### 固定脚本策略

后续局部补充手册时，固定保留并复用已有脚本，不要每次重新生成新的页面脚本：

- 通用写回脚本固定使用 `scripts/update_manual_sections.py`
- 聊天原图提取固定优先复用 `scripts/extract_chat_images_from_codex_log.py`
- 每次任务只新增或修改很小的 `scripts/manual-*.json` 配置文件
- 如果已有配置文件可继续复用，优先直接改原配置，不重新生成同类 Python 脚本

除非本次需求已经超出通用脚本能力，否则不要再新增一次性的 `scripts/update_manual_xxx.py`。

### 最终版式规则

局部章节默认优先按 `全局配置`、`任务中心` 这类已经确认过的版式组织内容，也就是：

```text
页面总说明
子模块标题
子模块整体说明
[图片]
图：图片说明。
字段/操作/影响范围说明
```

推荐固定顺序：

1. 页面总说明
2. 子模块标题
3. 子模块整体说明
4. 图片
5. 图注
6. 字段说明、关键操作、影响范围、保存操作、使用要点

如果一个章节里有多个主表、弹窗或按钮弹窗，优先直接在 JSON 配置里按线性 block 写死顺序，也就是 `submodule_title -> paragraph -> image -> bullet...`；不要为了省几行配置把顺序交给隐式结构推断，避免文档里出现“图先于整体说明”或“说明顺序被打乱”的情况。

如果一个章节已经在 Word 里放了多张图，但这些图当前只是堆在章节末尾、没有跟随对应说明，仍然视为“版式未完成”：

- 不要保留原位置后再往标题下面补文字。
- 不要指望通用脚本自动把旧图移动到正确段落。
- 固定改为“显式图片 block 重建整节”，让每张图都跟在对应子模块说明后面。

### 页面说明写法

页面正文不要再使用过于空泛的占位标签，例如：

- `页面内容`
- `关键操作`

除非实在没有更准确的写法，否则优先改成页面里真实存在的字段、动作或影响范围，例如：

- `配置状态`
- `领取限制`
- `影响范围`
- `保存操作`
- `使用要点`
- `通道状态`
- `手续费规则`
- `提现限额`
- `分组名称`
- `图标与排序`
- `并发订单`
- `提示语内容`

写作原则：

- 直接按当前页面说明，不写抽象套话。
- 优先解释图上真正能看到、能修改、能保存、会影响前台或后续流程的字段。
- 配置页、弹窗页重点写字段含义、开关状态、保存结果、影响范围。
- 报表页重点写关键字段、统计口径、筛选条件和导出结果。
- 不要反复写“页面内容：页面支持……”这种空转句式；应尽量写成“某字段/某操作：作用 + 影响 + 保存前检查点”。

### 文件占用处理

当 `B端后台操作手册.docx` 或目标 Word 文件被占用、脚本保存时报 `PermissionError` 时，固定处理规则如下：

1. 立即停止。
2. 只提醒用户“文件被占用，请关闭后我再继续”。
3. 不继续生成临时副本。
4. 不继续写入其他备用 docx。
5. 不继续反复重试保存。
6. 等用户明确回复“继续”后，再重新执行写回。

除非用户明确要求保留一个临时副本给他先看，否则遇到占用时不要自行做任何替代写回动作。

## Word 格式规范

默认样式：

- 全文中文和英文优先统一为 `微软雅黑`。
- 一级章节：`Heading 1`，约 14 号，加粗，深蓝色。
- 二级标题：`Heading 2`，约 12.5 号，加粗，深蓝色。
- 三级标题：`Heading 3`，约 10.5 号，加粗，深蓝色。
- 正文：10 号，常规。
- 字段说明中冒号前文字：10 号，加粗，深蓝色。
- 图片说明：图片下方居中，约 9 号，灰色。
- 图片宽度：常规后台横向截图约 6.25 到 6.5 英寸，避免超出页边距。

如果局部章节已有更明确的样式，优先保持局部风格。只有用户明确要求统一全书时，才全局调整样式。

## 图片处理规范

- 图片必须嵌入 Word，不能只写本地路径。
- 图片资产放入 `custom/assets/` 下的专门目录。
- 每个页面或模块单独建目录，推荐命名为 `custom/assets/manual-页面或模块英文短名/`；图片文件名使用小写英文、数字和连字符，表达页面或区域用途，例如 `page-overview.png`、`edit-modal.png`、`list-table.png`。
- 每张图片下方必须有说明文字。
- 图片说明要说明图片对应的页面、配置卡片或操作区域。
- 已有图片的含义以原文档为准，不确定时先看图片左上角 title、图片下方图注和旧文档所在章节，不要凭当前 Word 的图片顺序重新命名。
- 全量重整时，图片归属优先按图注编号、图片左上角 title 和旧文档位置映射到对应页面；当前 Word 可能已经被重排过，不能只按当前图片顺序抽取。
- 保留已有截图时，不要重新生成替代图，除非用户明确要求。
- 如果从 HTML 页面生成示意图，视觉应贴近 B 端后台：白色卡片、低饱和蓝按钮、紧凑表格、清晰层级。
- 图片来源优先级分两层：
  - 聊天原图场景：当前会话原图 > 当前线程历史原图 > 本机 Codex 日志中的原图 > 用户重新上传或提供路径。
  - 非聊天原图场景：原 Word 中已有截图 > 本地 HTML 原型截图 > Pillow 等方式绘制的示意图。
- 用户把截图直接发到聊天时，优先从当前线程消息中直接提取图片，不要先要求用户另存为本地文件，不要先截图 HTML，也不要先生成示意图。若图片项是 `data:image/...;base64,...`，直接解码保存到 `custom/assets/` 对应手册图片目录，再用 `python-docx` 嵌入 Word。
- 从聊天消息提取图片时，保存后必须检查图片格式、尺寸和内容是否与用户截图一致；如果只能找到缩略图、提示气泡图或无关缓存图，不能用于手册，应继续从线程消息的图片数据源提取。
- 只有在线程消息中没有可读取图片数据、也无法从本机 Codex 记录中定位对应 `data:image` 内容时，才请用户提供图片文件路径或重新上传截图。
- 用户明确要求“只使用我聊天发送的图加入文档”时，只能使用聊天中发送的截图作为手册配图；即使本地 HTML、旧 Word 截图或后绘制示意图更完整，也不能自行改用其他图片来源。
- 如果聊天截图在提取、读取或工具输出中出现截断，正文仍按该截图对应页面补充，文档中保留该图片的图注和占位说明，等待用户后续手动把完整图片放入 Word；不要因为截图截断而擅自改用别的图源补位。
- 只有在用户没有提供聊天原图，且旧 Word 中也没有可复用截图时，才考虑从本地 HTML 截图或用 Pillow 复刻示意图。
- 用 Pillow 绘制示意图时，不追求像素级还原，但必须保留真实页面的标题、主要按钮、字段、表格列、开关状态、配置卡片和弹窗结构；不要画与页面无关的装饰图。
- 图片应服务于说明内容：页面概览图放在页面介绍后；弹窗图放在新增、编辑、保存等流程说明后；多 Tab 页面按页面或截图顺序分别放图。
- 常规横向后台截图插入 Word 时宽度控制在 6.25 到 6.5 英寸；竖向弹窗或窄图按内容比例缩小，避免超出页边距或压缩到不可读。
- 图片段落居中，图注段落紧跟图片下方并居中，图注字号约 9 号、灰色。图注使用“图：页面或区域说明。”这种稳定格式；除非整本文档已有连续编号要求，否则不要手工维护复杂编号。
- 图片嵌入 Word 后，源图片也要保留在 `custom/assets/` 专门目录中，便于后续替换、审查和重跑脚本。
- 局部补图不要重新抽取或重排全书图片；只处理目标章节范围内的图片和图注。
- 插入图片前后要检查图片数量变化是否符合预期；替换图片时图片总数可能不变，新增图片时数量应增加。

### 会话图片提取固定流程

适用场景：

- 用户把截图直接发在当前 Codex 会话里。
- 用户要求“只用我发的图”更新操作手册。
- `read_thread` 工具界面疑似把长 `data:image` 内容截断。

固定优先级：

1. 先读当前线程历史，不先翻浏览器缓存、`blob_storage`、`Local Storage`。
2. 线程工具输出如果被截断，再查本机 Codex 日志里的 `thread/read` 响应。
3. 只有线程历史和本机日志都拿不到完整 `data:image` 时，才请用户重发或给本地路径。
4. 只要线程或日志里已经拿到完整聊天原图，就必须直接落盘、核对、嵌入 Word，并停止继续寻找替代图源；不得再改用 HTML 截图、Pillow 重绘或其他示意图。

固定默认动作：

1. 先判断这次是不是“聊天原图场景”；只要当前轮或当前线程里存在用户截图，就按聊天原图场景处理。
2. 第一优先动作固定为 `thread/read`：先读取当前线程最近几页，定位真正带图的 `userMessage`，优先直接使用线程工具返回的完整 `data:image`。
3. 一旦 `thread/read` 已返回完整图片数据，立即交给可重跑脚本从该条 `userMessage` 批量落图；不要再去扫缓存、日志或手抄 base64。
4. 只有当 `thread/read` 结果被截断、工具输出不完整、或无法稳定取到完整 `data:image` 时，才切到本机 Codex 日志里的 `thread/read` 响应做第二步提取。
5. 如果日志提图脚本因消息文本匹配、日志截断或输出范围问题失败，再做最小范围日志定位；不要继续扩展更复杂的提图分支。
6. 核图确认无误后，再复制或重命名为语义化文件名，例如 `page-overview.png`、`list-table.png`、`edit-modal.png`。
7. 手册更新脚本最终只引用语义化原图文件，不直接依赖临时 `chat-序号.png`，也不保留 fallback 图源分支。

本次已验证的主要问题与结论：

- `thread/read` 有时已经能一次返回完整 `data:image`；如果此时还先走日志脚本，会多一次定位和匹配，纯属额外耗 token。
- 旧的日志提图脚本依赖“线程 id + 消息文本”去日志里反查，命中条件偏脆，遇到消息文本变化、转义差异、日志字段变化时容易失配。
- 在对话里手工搬运长 base64 风险高，容易截断、复制不全或落成坏图；拿到完整 `thread/read` 或日志响应后，应直接让脚本解码写文件。
- 最省 token 的顺序不是“先全局搜日志/缓存”，而是“先 `thread/read`，不够再日志”；因为线程工具天然已经把范围缩到了目标线程和目标消息。
- 脚本复用的关键不是每次重写一次性提取逻辑，而是固定输入源顺序和固定输入参数，保证脚本能重复消费同类 `thread/read` / 日志响应。
- 再往下叠更复杂的兼容提取收益很低；两步都失败时，直接在文档里保留图片占位和图标标注，比继续深挖更省 token，也更稳。

推荐流程：

1. 先用 `thread/read` 读取当前线程最近几页，定位真正带图的那一轮 `userMessage`；不要假设最新一轮就有图。
2. 在该轮 `userMessage.content` 中查 `type":"image"` 或 `data:image/png;base64,`，先确认有几张图，再决定 `expected-count`。
3. 如果 `thread/read` 已返回完整 `data:image`，最省 token 的固定做法是：直接把这条 `userMessage` 作为脚本输入源，由脚本一次性解析并落盘全部图片；到这里就停止，不再额外搜索本机日志、缓存或浏览器存储。
4. 如果 `thread/read` 返回被截断，第二步才去本机 Codex 日志中找同一次 `thread/read` 的完整响应。搜索关键字只保留这几类：
   - 线程 id
   - `method=thread/read`
   - `Sending server response`
   - `data:image/png;base64,`
5. 在日志中找到目标 `Sending server response` 后，优先截取“目标 `userMessage` 起点到下一条消息项起点”的最小范围，再一次性提取该范围内全部 `data:image/png;base64,`；不要只取第一个匹配结果。
6. 解码后先落到 `custom/assets/manual-模块名/` 或 `custom/assets/manual-模块名-original/`，再逐张检查内容、尺寸、顺序和数量。
7. 只有确认图片与用户会话中的截图一致后，才重命名为语义化文件名并嵌入 Word。
8. 若同一页面的聊天原图已经确认无误，后续更新脚本只能引用这些原图文件；不要在同一脚本里混入“原图缺失时自动重绘”或“自动截 HTML 兜底”的分支。
9. 聊天原图提取优先复用现成脚本，不要每次临时重写一次性脚本；当前 `scripts/extract_chat_images_from_codex_log.py` 已同时支持两种输入源：第一步直接读取保存下来的 `thread/read` 结果，第二步回退到本机日志里的 `thread/read` 响应，两种模式都保持同一套参数口径：线程 id、目标消息标识、输出目录、期望数量。

最省 token 的固定顺序：

1. `thread/read`
2. 脚本直接读取目标 `userMessage`
3. 仅在 `thread/read` 截断时，才切日志版脚本

不要反过来先写日志脚本再去猜消息，也不要先扫缓存；这两种都更费 token，而且更脆。

禁止动作：

- 不要在用户已发聊天截图的前提下先生成 Pillow 示意图。
- 不要在用户已要求使用聊天原图时先去截图本地 HTML。
- 不要在原图尚未排查完线程和日志前，就把“示意图”“旧 Word 图”“HTML 截图”写进文档。
- 不要把“提图失败时自动重绘”写进手册更新脚本。

省 token 规则：

- 不先全盘搜索 `AppData`、浏览器缓存、Session Storage、IndexedDB、`blob_storage`；这类路径命中率低，输出噪音大，通常比直接查线程和日志更费 token。
- 不反复读取整条长日志；先用线程 id 或 `method=thread/read` 定位到行号，再只看相邻少量内容。
- 不重复打印完整 base64 到对话里；直接在脚本里解码保存，再验证文件。
- 不在拿到第一张图后就停；先确认该条 `userMessage` 里有几张图，再一次性全部提取。

多图场景规则：

- 同一条 `userMessage` 可以连续带多张 `type":"image"`，不能默认只有 1 张。
- 日志里一个 `ASUVORK5CYII=` 结束后，后面可能紧跟下一张 `data:image/png;base64,`；不要把第一张图的结束位置误判为整条消息结束。
- 提取范围优先按“该条 `userMessage` 起点到下一条 `agentMessage` 或下一条消息项起点”截取，再在该范围内批量匹配全部 base64 图片。
- 批量落图后先用 `view_image` 或等效方式逐张核对，再决定文件名映射。

推荐命名流程：

1. 先保存为 `chat-1.png`、`chat-2.png` 这类临时名。
2. 核对内容后，再复制或重命名为 `page-overview.png`、`edit-modal.png`、`list-table.png` 等语义化文件名。
3. 手册脚本默认引用语义化文件名，不直接依赖 `chat-序号.png`，避免以后会话顺序变化导致错图。

推荐脚本用法：

```powershell
python scripts\extract_chat_images_from_codex_log.py --thread-id "线程id" --message-text "用户该条消息文本" --output-dir "custom/assets/manual-模块名-original" --expected-count 2
```

多图场景可直接提高 `--expected-count`，脚本会按 `chat-1.png`、`chat-2.png`、`chat-3.png` ... 批量落盘；核对完成后再复制或重命名为语义化文件名。

如果已经把 `thread/read` 的结果保存为本地文件，优先用这一种：

```powershell
python scripts\extract_chat_images_from_codex_log.py --thread-read-file "thread-read.json" --message-text "用户该条消息文本" --output-dir "custom/assets/manual-模块名-original" --expected-count 2
```

只有 `thread/read` 文件内容不完整或工具结果被截断时，才退回上一条日志模式命令；如果日志模式也失败，就停止继续扩展提图逻辑，改为在文档对应位置保留图片占位和图标标注。

失败兜底：

- `read_thread` 没拿到图，不代表会话里没有图；先继续翻旧页或查日志。
- 如果日志里只有截断内容，正文可以先补文字，并在放图位置保留占位和图标标注；不要换成 HTML 截图或重绘示意图补位。
- 只有在线程历史、旧页、日志三条路径都失败且用户明确要求必须补齐原图后，才请用户重新发图。
- 用户已明确要求“按聊天原图入文档”时，未完成线程和日志两步排查前，禁止自行进入任何 fallback 图源。

说明：

- 之前有时“不麻烦”，通常是因为线程读取工具直接给了完整 `data:image`，可以一步解码。
- 变麻烦的主要原因不是图片本身，而是工具输出截断、会话翻页后图片不在最新一轮、或同一条消息里有多张图。
- 因此默认流程应该是“先线程，后日志，不扫缓存；先整条带图消息，后逐张落盘”，不要每次重新探索。

## 脚本与编码规范

- 推荐使用 `python-docx` 修改 Word。
- 生成图片可使用 Pillow 或浏览器截图工具。
- 修改 Word 前必须备份，推荐带时间戳备份。
- 如果保存 Word 时遇到文件被 Word/WPS 打开占用、`PermissionError`、权限拒绝或类似写入失败，立即暂停并提示用户关闭文件；不要继续执行进程检查、重复写入、额外校验或其他操作。等用户确认已关闭后再继续。
- 中文内容、中文文件名和脚本均按 UTF-8 处理。
- 不要用 PowerShell here-string 写包含大量中文的脚本或文档内容。
- 需要处理中文路径或中文关键字时，优先写 UTF-8 `.py` 脚本，再运行脚本。
- 写入后验证关键中文文本可读取，不出现真实 `?` 码点损坏。
- 不要重复打印完整 Word 段落，除非正在排查结构损坏。
- 局部更新默认优先复用 `scripts/update_manual_sections.py`；把本次章节内容写进一个很小的 UTF-8 JSON 配置文件，再执行一次通用脚本，不要为每个页面重复造一次性脚本。
- `scripts/update_manual_sections.py` 默认负责：章节定位、删除旧正文、按统一样式写入正文、按配置决定是否保留旧图或重插本地图片、备份和保存。
- 通用脚本有两种模式：一是显式图片 block 重建整节；二是保留原媒体只补文字。默认优先选前者，只有在图片位置已经正确且无需调整顺序时才选后者。
- 如果目标章节的图片虽然已经在 Word 里，但位置还不符合“说明 -> 图片 -> 图注 -> 要点”的最终版式，必须先提取/落盘这些图片，再把 `keep_existing_media` 设为 `false` 重建整节；不要继续保留旧图位置。
- 多图章节、含主表加弹窗的章节、需要按图分段说明的章节，默认不用 `keep_existing_media: true`。
- 如果图片来自当前聊天，优先让 `scripts/update_manual_sections.py` 配置里的 `chat_image_tasks` 复用 `scripts/extract_chat_images_from_codex_log.py`；先落盘聊天原图，再由同一个通用脚本写回 Word。
- 只有当通用脚本无法覆盖本次特殊需求时，才新增独立页面脚本。
- 局部更新脚本应把正文内容、图片生成、备份、章节定位、旧内容删除、图片嵌入和保存放在同一个可重跑脚本里，避免手工点选 Word。
- 但当图片来源被限定为“聊天原图”时，脚本中的图片阶段必须改为“提取/引用原图”，不能保留自动生成示意图的代码路径。
- 对后续高频页面，优先把“提图”和“写回 Word”固定成可重跑脚本；避免下一次又从零分析会话图片来源。
- 脚本定位章节时优先按精确标题和样式查找，例如 `Heading 3` 的页面名；删除旧内容时只删除目标标题到下一个同级或更高标题之间的段落。
- 用 `python-docx` 插入图片时，先新增居中图片段落，再紧跟新增图注段落；不要只在文档中写图片路径。
- 如果脚本生成图片，先确保资产目录存在，再保存图片；图片生成失败时不要继续写入 Word。

## 快速检查命令

检查一级标题和图片数量：

```powershell
python -c "from docx import Document; d=Document('B端后台操作手册.docx'); [print(f'{i:04d}\t{p.style.name}\t{p.text.strip()}') for i,p in enumerate(d.paragraphs) if p.style.name in ('Heading 1','Title') and p.text.strip()]; print('images', sum(1 for p in d.paragraphs for r in p.runs for _ in r._element.xpath('.//w:drawing')))"
```

检查关键中文是否存在：

```powershell
python -c "from docx import Document; d=Document('B端后台操作手册.docx'); s='\n'.join(p.text for p in d.paragraphs); terms=['风控管理','人事中心','站点设置','首页看板','模板管理']; [print(t, s.count(t), ' '.join(hex(ord(c))[2:] for c in t)) for t in terms]"
```

检查脚本语法：

```powershell
python -m py_compile scripts\reorganize_manual_docx.py
```

通用脚本 dry run：

```powershell
python scripts\update_manual_sections.py --config scripts\manual-sections.sample.json --dry-run
```

通用脚本正式写入：

```powershell
python scripts\update_manual_sections.py --config scripts\manual-本次任务.json
```

## 最小校验清单

完成编辑后至少检查：

- Word 能被 `python-docx` 正常读取。
- 一级章节顺序符合当前目录骨架，除非用户明确要求调整。
- 页面归属符合后台真实菜单，不因业务场景归类而移动页面。
- 图片数量符合预期。
- 已有截图没有被误删。
- 新增或替换的图片能在 Word 中作为嵌入图片被统计到，源图片也保留在 `custom/assets/` 对应目录。
- 图片图注存在、位置紧跟图片下方，且描述与目标页面或配置区域一致。
- 关键中文标题读取正常，没有乱码。
- 目标章节已更新，后续章节未被误删。
- 如果修改了脚本，运行 `python -m py_compile`。
- 备份文件存在，可以回退。

## 省 token 执行原则

- 先读本规则，再操作 Word。
- 先判断是否属于“聊天原图场景”；如果是，先提图再写文，不先做页面分析扩散。
- 大目录调整优先改全量脚本，不重新摸索 Word XML。
- 如需确认菜单顺序，只读取必要的菜单片段或 Word 标题，不全量打印。
- 局部补页面只读目标章节附近段落，不全量打印。
- 局部补页默认先写最小 JSON 配置，再跑 `scripts/update_manual_sections.py`；不要先复制旧脚本再改标题和正文。
- 对“图片已在文档里”的场景，优先保留原图并只替换正文；对“图片在聊天里”的场景，优先让通用脚本复用聊天提图参数，不单独再写一次提图脚本。
- 已知目录口径不要反复讨论，除非用户提出新的口径。
- 不把具体模块正文、字段描述、FAQ 答案写进本规则文件。
- 不在会话里重复证明“为什么不用别的图”；规则已确定后，直接按固定流程执行。
- 不做浏览器预览，除非用户明确要求。
- 不做无关全项目扫描。

## 回复用户

回复保持简洁，通常包含：

- 已修改的文档路径。
- 更新了哪些章节或模块。
- 是否保留或插入图片，图片资产放在哪里。
- 已做的基础校验。
- 备份文件路径。
